apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: petadoption-production
  namespace: default
spec:
  secretName: petadoption-tls
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
  dnsNames:
    - petadoption.ferdeve.fit